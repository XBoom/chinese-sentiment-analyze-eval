# 说明：
# - macOS x86_64 等平台 PyPI 上 torch 常见最高为 2.2.x；transformers 5.x 往往要求 torch>=2.4，故 transformers 锁定 4.x。
# - transformers>=4.45 可能对 torch.load 强制要求 torch>=2.6；在 torch 2.2 环境下请保持 transformers<4.45。
# - datasets>=3 对部分 Hub 旧式 dataset 脚本不兼容，故 datasets 锁定 2.x。
# - torch 2.2 与 numpy 2.x 组合易告警，故 numpy<2。
torch>=2.0.0,<2.3
transformers>=4.36.0,<4.45
tokenizers>=0.19,<0.20
datasets>=2.16.0,<3
accelerate>=0.25.0,<1
numpy>=1.24.0,<2
scikit-learn>=1.3.0
python-dotenv>=1.0.0
